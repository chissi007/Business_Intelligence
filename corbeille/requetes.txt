1. creer la base "covid"
CREATE DATABASE IF NOT EXISTS covid

2. creation des tables

CREATE TABLE utilisateur
(
    id INT PRIMARY KEY NOT NULL,
    nom VARCHAR(100),
    prenom VARCHAR(100),
    email VARCHAR(255),
    date_naissance DATE,
    pays VARCHAR(255),
    ville VARCHAR(255),
    code_postal VARCHAR(5),
    nombre_achat INT
)

#table genre
CREATE TABLE genre
(
    id_genre INT PRIMARY KEY NOT NULL,
    sexe VARCHAR(10)
    
)

#defir cle primaire
 ALTER TABLE `periode_ep` ADD PRIMARY KEY(`id_periode`);

ALTER TABLE semaine
ADD 
    FOREIGN KEY id_periode             -- La colonne sur laquelle on ajoute la clé
    REFERENCES periode(id_periode);  -- La table et la colonne de référence

___
ALTER TABLE semaine ADD FOREIGN KEY (id_periode) REFERENCES periode_ep(id_periode)

ALTER TABLE date ADD FOREIGN KEY (id_semaine) REFERENCES semaine(id_semaine)

ALTER TABLE departement ADD FOREIGN KEY (id_region) REFERENCES region(id_region)

UPDATE `date` SET `date` = '2020-03-18' WHERE `date`.`id_date` = '1';